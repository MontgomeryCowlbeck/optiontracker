# Environment files with secrets
.env

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/

# Data / DBs — never commit (real trade data; regenerated by sync)
data/*.db
*.db
*.db-journal
*.db-wal
*.sqlite

# Source archive — never commit the 124 MB import zip
optiontracker.zip

# Local secrets bridge (we source creds from /projects/quant/.secrets instead)
.env
.env.local

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db
nul

# Node
node_modules/
.pytest_cache/
